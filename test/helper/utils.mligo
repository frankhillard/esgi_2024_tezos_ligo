// TODO: Please implement me
// zipWith takes 2 lists (A and B) and a function "f" as parameters and returns a list with function f applied to elements of list A and B at the same index
// for example 
// the A list is ["a"; "b"; "c"; "d"; "e"]
// the B list is ["A"; "B"; "C"; "D"; "E"; "F"]
// the function "f" concatenates the elements from A and B
// the expected result is  ["aA" ; "bB" ; "cC" ; "dD" ; "eE"]
let zipWith = 
